import styled from 'styled-components/native';

export const HighlightsListContainer = styled.View`
  width: 100%;
  margin-top: ${({theme}) => theme.screen.rem(2)}px;
`;

export const CategoryListContainer = styled.View`
  margin-top: ${({theme}) => theme.screen.rem(2)}px;
`;
